# Lucide GPU title icon

The user selected the [Lucide GPU icon](https://lucide.dev/icons/gpu) on
2026-09-12 and supplied it as an SVG data URL. `gpu.svg` preserves the exact
decoded bytes, without a byte-order mark or added newline.

`GPU.ini` adapts its 24-unit viewBox into a native Rainmeter Shape meter.
Coordinates, 2-unit strokes, round caps and round joins scale uniformly by
`TitleIconSize / 24`, where `TitleIconSize = 14 * TitleIconScale` and
`TitleIconScale = Scale * TitleFontSize / 10`. The canvas stays centered on
the shared title row, and `currentColor` maps to the configurable `GPUColor`.
Rainmeter renders the native shapes; this SVG is retained for provenance and
adds no runtime plugin, network request or polling.

Lucide uses the ISC license. The complete upstream license, verified at
[revision a79b2d131dab2bf20cb224bd0937b439a9c4fa99](https://github.com/lucide-icons/lucide/blob/a79b2d131dab2bf20cb224bd0937b439a9c4fa99/LICENSE),
is preserved in `LICENSE.txt`, including the additional Feather MIT notice.
GPU is not among that notice's listed Feather-derived icons. The supplied
SVG's own upstream revision is unknown; the pinned revision identifies the
license source only.

SHA256:

- `gpu.svg`: `A33475E2A6A9233821C1753DF33B5E6C64AEA08DB9FA58EBF84D4AD3E4AB863C`
- `LICENSE.txt`: `B495047BD93A9B06913511076F504DABA17D5BBEB3E0650F3BB53A4220329C57`
