# Lucide Visualizer icon

The user selected the [Lucide Speaker](https://lucide.dev/icons/speaker) icon on 2026-09-13 and supplied it as an SVG data URL. The decoded SVG markup is retained in `speaker.svg` for provenance.

`View.inc` adapts the 24-unit drawing into a 14-logical-pixel native Rainmeter Shape beside the current output-device name. Its rectangle, circle, short paths, two-unit stroke and rounded caps/joins scale uniformly by `14 * Scale / 24`; `currentColor` maps to `MediaColor`. Rainmeter does not load the SVG at runtime.

Lucide is distributed under the ISC license. The full notice is retained in `LICENSE`. This icon adds no plugin, font, script, runtime network access or polling.

SHA256 of the decoded user-supplied SVG:

- `speaker.svg`: `8F578D9AADB55DEFA7C1EBFF1C1391FEB5B97CE0D868DE44F67B7C84F54E9B79`
