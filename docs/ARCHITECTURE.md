# Parallax shared contract v2

## Ownership and scope

Each utility maintains its current required, bundled and optional dependencies in the Dependencies section of its module document. Update that list with implementation changes and document unavailable-feature behavior. [DEPENDENCIES.md](DEPENDENCIES.md) indexes those lists and the shared platform requirements; Global Settings and ColorPicker have entries in their integration-owned documents. Planned integrations remain separate from current dependencies.

The original task owns global settings, shared appearance/geometry, integration and packaging coordination. Eight dedicated tasks own Chronometer, CPU, RAM, GPU, IO, Network, Media and Visualizer. Media owns optional Spotify queue; Visualizer is a separately loaded output-device spectrum companion. IO owns disk capacity/transfer rates. Network is independently loadable and owns NIC throughput, adapter selection and traffic history.

The separate Welcome config is an integration-owned utility and the config the `.rmskin` loads after installation. It lists the eight utilities and Global Settings as check boxes, toggling each one with `!ActivateConfig` / `!DeactivateConfig`, and offers Load all and Unload all. It holds no preference of its own: loaded state is read from Rainmeter's own settings file on explicit events only, and each row matches its config's recorded skin-file position, so `Media\Setup.ini` does not check the Media row. See [WELCOME.md](WELCOME.md); adding an alphabetically earlier `.ini` to a listed config folder changes that position and must be reflected in `Welcome.lua`.

The separate ColorPicker config is a global-settings utility owned by the integration task. One reusable RGB/HSV/CIELAB D50 picker handles nine allowlisted targets: `AccentColor`, `AccentColor2`, `TitleTextColor`, `HeaderTextColor`, `TextColor`, `BackgroundColor`, `BorderColor`, `DividerColor` and `TableHeaderBorderColor`. Preview remains local until Apply writes only the selected key, refreshes Parallax and closes the picker. Background edits preserve its existing alpha; other targets save opaque RGB. Cancel writes nothing. Twelve numeric Global Settings fields use the reviewed one-shot SettingsInput helper and fixed data-only result protocol; neither settings utility adds recurring collection work.

Implement original assets and code. ModernGadgets is design inspiration, not an imported dependency. The user's revised visual direction is much closer to its dense black instruments; follow `docs/APPEARANCE.md` revision 2 for the current contract. All colors remain configurable.

User-selected Media title, source, metadata, queue and transport icons are attributed Lucide exceptions. Native Shape meters adapt thirteen SVGs; exact source bytes, provenance and full Lucide ISC/Feather MIT notices live in `@Resources/Modules/Media/Icons/Lucide/`. Player and Setup show Monitor Play beside the static `Media Player` title at the shared title-row center. Its paths and strokes scale into `TitleIconSize`; title text starts after the icon and shared gap, with width reserved for the right gear and clearance. In the double-width layout, a fixed 97 px title allocation is followed by the 14 px Audio Lines icon and body-style current-player name on the same centerline, ending 24 px before the gear. The compact layout retains its stacked 18 px player row because the narrow maximum-font endpoint cannot contain both identities on one line. The source name remains passive literal data with a full-name tooltip. Metadata and queue artwork also use 14 px canvases; the queue icon uses an 18 px target and switches List Plus/List Minus with the existing persisted expansion choice. Label, icon and status all invoke that same toggle.

Media cover art keeps its top-left anchor and adds 20 logical px to the preceding 75%-size double-width square. At standard width and scale it is 169 px square; compact artwork remains 36 px. In the double-width layout, metadata, progress and transport controls move 20 px upward while preserving their column bounds and 24 px metadata pitch. Progress begins 8 px after the artwork and ends at the same right edge; the three-button group recenters in the remaining span before visible timing text. The compact layout retains its prior vertical positions. The normal width 180/220 queue anchors remain unchanged; unusually wide custom panels extend the body only when needed to prevent the larger artwork from colliding with the queue. With width 220, scale 1 and the default 6 px data bar, collapsed windows remain 456×222 px at double width and 228×210 px in compact layout. This revision changes no gear, playback behavior, data-bar contract, queue preference or provider cadence.

Media playback artwork uses 24 px vectors within 28×26 px targets. The three-button 92 px group centers between the artwork's right edge (content origin in compact layout) and the measured left edge of visible time text; timing width is capped to reserve control clearance. Dimensions scale with the suite, and layout updates are deduplicated. Enabled Previous/Next fill on hover; the center uses Step Forward while hovering Pause and Play Off while hovering Play. Capability checks and playback actions remain unchanged, including Pause on click despite its hover artwork. Hover alone never dispatches playback. These icons require no plugin, icon font, runtime network access or new polling.

Media Settings keeps its 498 px collapsed / 554 px expanded layout. Three numeric controls use arrows with integer steps of one and validated typed centers: Columns 1–2, queue rows 1–5 and queue polling 30–150 seconds. Bound and unchanged actions save or refresh nothing. Collapsed queue details remove all frame, arrow, value and label hit bounds, while the toggle, shared sampling and recovery actions stay accessible. Explicit typed entry uses the shared one-shot helper; changing the interval retains the existing restart reminder rather than changing provider lifecycle.

A fresh, valid ready queue snapshot with no items now collapses an expanded inline queue through its existing fixed callback, saves `QueueExpanded=0` and refreshes the Media layout and open settings. The request is deduplicated per confirmed-empty transition, including failed preference writes. Missing, expired, malformed, error and setup states do not count as an empty queue. Later nonempty data does not re-expand the section. The standalone Queue panel and provider sampling remain unchanged. See [Media settings and queue validation](modules/Media.md#validation-report) for the separate rendered, controller and helper checks.

Media's optional providers now remember explicit enablement in private `enabled.intent` files outside skins. Once per normal Rainmeter view load, `MediaLifecycle.lua` resumes Source and Queue for Player, or Queue alone for Setup/standalone Queue. Settings has no lifecycle include. Recovery requires the standard `%APPDATA%\Rainmeter` settings profile; isolated previews and custom/portable profiles remain inert and retain explicit Start actions. Missing, disabled, malformed or unreadable intent and persistent Stop markers prevent recovery. Existing tokens or snapshots never imply enablement. Resume opens no consent flow, preserves queue retry/quota barriers and uses the current validated queue interval. The existing collectors retain their cadence; per-provider control/worker mutexes and Queue's launch permit prevent duplicate or superseded collectors. No service, scheduled task or polling launcher is added. See [Media recovery semantics](modules/Media.md#provider-recovery-after-restart) for enable/disable commands, older-installation migration and Connect/Stop limitations.

The user-selected GPU, RAM and Disk Meter title icons are also attributed Lucide exceptions. Their native Shape adaptations map the supplied 24-unit SVGs uniformly into `TitleIconSize`, inherit `GPUColor`, `RAMColor` or Disk Meter's `MutedColor`, and share the title-row center. Exact source bytes and full notices ship under each module's `Icons/Lucide/` directory without adding a runtime dependency or polling.

Chronometer Settings has nine shared arrow/value groups: six numeric controls for Columns, adjustment step and four visible durations, plus wrapping clock-format, date-format and list choices. Columns accepts integers 1–2, step accepts exactly 1/60/300/3600 seconds and durations accept 1–604800 whole seconds. Numeric arrows clamp; duration arrows use the selected step. The fixed shared-helper result is checked against bounds, step membership, captured list/slot and unchanged saved value. A duration edit resets only that timer. Hidden Clock/Event/Timers details reclaim 84/28/216 px across 47 components, preserving headings and re-enable controls; settings height ranges 424–752 logical px without changing the monitor's saved geometry. Final 88 controller checks and 25,181 native checks across three cases passed. Independent source agreement and two maximum-font all-visible settings captures also passed; actual editor focus, physical input, mixed DPI and performance remain unverified. See [Chronometer validation](modules/Chronometer.md#validation-record).

CPU Settings now follows the Global Settings form with five Accent 1 sections: Appearance, Processes, History, Performance and HWiNFO setup. Its independent double-column height ranges from 888 to 1000 logical px. Seven validated integer controls accept process count 1–10, history capacity 10–300, percentage and temperature decimals 0–1, voltage decimals 0–3, CPU refresh 500–2000 ms and sensor reads 500–5000 ms. Arrow steps are one for counts/precision, ten for history and 500 ms for rates; endpoints make no write or refresh. History source and Search scope wrap, retaining the latter's existing identity reset and scan semantics. Named exports, Thread Colors, setup commands and toggles keep their roles.

CPU's hidden process list reclaims the 28 px process-count row; hidden History reclaims 56 px for source/capacity. Thread Colors reclaims 28 px only when neither the table nor a visible per-thread graph uses it. Headings, Show switches and setup/recovery stay accessible, as do precisions used by the always-visible total and sensor footer. Hidden components have zero native bounds and later sections reflow without overwriting main dimensions. Final source/controller review, 42 Lua cases, 45 source layouts and 2,400 native assertions across narrow/default/200% endpoints passed. Actual numeric popup and provider actions were mocked in this focused check. See [CPU settings validation](modules/CPU.md#settings-structure-collapse-and-stepper-validation-2026-09-12).

Disk Meter uses `IO/IO-Disk.ini` as its sole entrypoint, preserving IO paths and existing preference keys. `IODiskDrives` selects C: by default, an explicit canonical A–Z list, all detected letters, or none. Each selected drive shows its letter/name above a shared-thickness used bar, with read speed, write speed and free/total capacity sharing one line below it; C: appears first, then other letters alphabetically. Explicitly selected disconnected drives retain unavailable rows. The header shows capacity-weighted usage only when all selected capacities are valid. Capacity and byte speeds use decimal KB/MB/GB; the existing byte/bit setting remains. Settings offers Drives, Graph/display and Capacity sections using the Global Settings field layout. The removed capacity-only entrypoint remains retired.

`IODriveNames=volume` adds Windows volume labels by default; `letters`, `model` and `both` are the other choices. Main and settings name rows keep one line, truncate long names with an ellipsis and expose the full sanitized name in a tooltip. Native FreeDiskSpace label probes share the inventory cadence. Model modes optionally invoke the bundled PowerShell/CIM helper once at config load, model-mode selection or explicit Refresh drives; the main and settings configs each hold their own in-memory cache. No model polling process or runtime output file is introduced. Windows model names may describe virtual devices, and unmapped volumes remain unavailable; cached models can become stale after hotplug or letter reuse until an explicit refresh. Name-only settings changes use a fixed main callback without reloading the monitor or resetting its graph observations.

Exactly one graph follows the readouts. `IOGraphMode=c` observes C: independently of visible selection; `combined` sums selected observations; `overlay` gives selected drives stable colors, solid read/dashed write traces and a wrapping legend. The fourth mode, `split`, sums selected reads above zero and writes below zero on a symmetric range with +/0/- markers, retaining the existing read/write colors. Negative write positions are a display convention; measured rates remain positive. Native A–Z type probes discover letter roots; enabled capacity/rate banks follow the selection, plus C: if the graph needs it. Mount-point-only volumes are outside this scope. The banks share UsageMonitor's existing one-second LogicalDisk worker and read/write datasets; disabling display banks does not promise worker suspension. Inventory/capacity normally samples every 30 seconds. There is no new polling process or runtime history file.

History retains up to 60 observation slots per drive, with no prefilling. At the user's request, current null/nonfinite/nonpositive observations plot at zero while rate text stays `--`; lines connect recorded points at their retained time positions. Combined and Split therefore sum available positive observations with zero placeholders, rather than certifying a complete physical-disk total. Graph-mode changes, selected membership, skin refresh or clock rollback reset history. All traces share 15% peak headroom and a 65,536-byte/s minimum, capped by the existing MiB/s-valued `IODiskMaxMiBs` key; UI labels convert the cap to decimal MB/s. Display cells can ellipsize long readings; full tooltips preserve rates, capacity and provider-freshness limits.

All drive-name modes share the same main geometry, including letters only. For a nonempty selection, let `extra=Max(0,DataBarThicknessPx/Scale-6)`: the first full-width name starts at y42, the bar slot at y60, and the shared speeds/capacity row at y68+extra. After two 4 px gaps, that row allocates 28% to read, 28% to write and 44% to right-aligned capacity, using single-line ellipsis and full tooltips. Drive blocks repeat every `50+extra` logical px; the graph starts at `89+extra+(count-1)*(50+extra)+legendHeight` and the derived minimum is `157+extra+(count-1)*(50+extra)+legendHeight`. A single C: drive has a 157 px minimum at default bar thickness in every name mode. An empty selection retains graph y89 and minimum 157 without extra bar space. Larger saved heights remain effective, and name-only changes alter neither geometry nor graph history. Settings retains its independent double-column width: the names field starts at y130 and drive rows at y184, with a 494 px one-visible-drive baseline and 28 px for each additional visible drive row. Explicit `none` reclaims the quota row's 28 px while retaining selectors and graph controls. These derived dimensions do not overwrite the main preferences. See [Disk Meter behavior and checks](modules/IO.md) for the bounded native banks, exact geometry and mode semantics.

Disk Meter Settings has four arrow/value groups. Names, graph mode and rate units wrap their choices, with forward cycling on the center; cap arrows choose the nearest strictly smaller/larger compatibility preset without wrapping. Its typed center accepts 0.0001–1,000,000,000 decimal MB/s with up to four decimals, then divides by 1.048576 for the existing MiB/s storage key. Cancel, invalid output, a concurrent cap change or accepting the unchanged rounded initial value preserves the original saved text. Unrepresentable saved values retain file editing. Only the explicit typed action launches the shared helper; categorical and preset actions add no process. Name changes retain their fixed callback and preserve history.

Network uses `Network/Network.ini` as its single monitor. One title and surface contain connection details above NIC traffic and the two histories: adapter identity/state, the PC-wide Internet report, address/gateway, reported link speeds, Wi-Fi status/signal/radio, then inbound/outbound readings and graphs. `Columns` sizes the complete monitor; saved `PanelHeight` remains a minimum. Its effective painted height is `Max(Round(PanelHeight*Scale),Round(464*Scale)+DataBarThicknessPx)`, giving 470 px at scale 1 with the default 6 px bar. Default-width windows are 228×478 single or 456×478 double. Legacy `NetworkConnectionColumns` and `NetworkConnectionHeight` remain preserved but ignored.

The former `Network/Connection/Connection.ini` is a compatibility redirect only: `Update=-1`, no measures and a transparent one-pixel bounds meter. Loading it activates the canonical Network monitor and unloads the redirect. Navigation and previews must not expose a second Connection utility. Independent settings remains 598 logical px (456×606 at default width/scale), with eight fields and one Show Network action. Six rows now use shared arrows: five numeric fields have validated typed centers, while units cycle bits/bytes; the Wi-Fi toggle and adapter file-editor action remain. Columns clamp at 1–2 and WLAN index at 0–63. Height and graph ceilings accept 0.0001–1,000,000,000 with up to four decimals; height steps by one and ceiling arrows move to neighboring presets without wrapping. Existing formulas, finer precision and exponent-form values retain file-editor access and are not rewritten automatically. Numeric entry launches the shared helper once on an explicit click; arrow actions add no process or telemetry query. Focused controller/native checks passed. Existing native collection banks retain their cadences inside the merged monitor, so loading Network includes the former companion's work. See [Network behavior, migration and checks](modules/Network.md) for source identity, input validation and resource-cost caveats.

Memory Meter retains RAM module paths and preference keys. `RAM: used/total` comes from native PhysicalMemory, with Available in its tooltip. `PAGE: used/allocated` now reports actual paging-file usage and current allocated capacity from Windows `EnumPageFilesW`, summed across a complete successful enumeration and multiplied by the OS page size. It excludes physical RAM and system commit accounting; current allocation can grow and is not the configured maximum. SwapMemory is no longer used. A successful empty enumeration shows No paging file; zero usage with positive allocation is valid. Startup, unsupported, failed and stale data retain explicit statuses. Per-module capacities, both reading pairs and process-memory values use decimal GB/MB divisors of 1,000,000,000 and 1,000,000. Existing `RAMUseMiB` choices remain compatible. The header percentage and history still represent physical RAM.

Memory Meter's refresh-only inventory query groups records by canonical manufacturer, exact per-record capacity, raw memory type and configured rate before formatting. Each distinct group wraps on its own line, with capacity beside the manufacturer; native wrapped height moves following content without saving a panel height. Nominal/derived clocks are labeled explicitly. Each RAM/PAGE reading has its own bar directly below it, followed by the five-row `Process / RAM` table and then History. Both bars use shared `DataBarThicknessPx`; `RAMShowBar` controls physical RAM and new `RAMShowPageBar=1` controls PAGE independently. The PAGE bar uses validated cached used/allocated bytes and hides for missing, stale, unsupported, no-file or zero-allocation states while keeping explanatory text and its layout slot. Minimum height is `242 + RAMInfoHeight + GraphHeight + 2*Max(0,DataBarThickness-6)`, honoring larger saved heights. The completed independent settings form remains 340 logical px with six controls: wrapping GB/MB arrows, two integer precision controls clamped to 0–2 with steps of one and validated typed centers, and separate RAM bar, PAGE bar and history toggles. No dependent details collapse behind these toggles. Process ranks remain same-name private-working-set groups with an independent one-second UsageMonitor worker. Cached formatting and reflow add no inventory query or history sample.

The PAGE provider uses one session bootstrap and one resident PowerShell/.NET host through bundled RunCommand, sampling at `MetricsInterval` clamped to 1–30 seconds. It calls Windows directly without a process per sample. Three token-scoped temporary siblings hold bounded atomic snapshots and a lease; no paging-file paths or filenames are published. The host has retained-ancestor and lease-expiry termination, and the controller tracks launched commands, refreshes their cached status and stops only owned commands currently running at status `0`. Source/ABI review, exact helper staging and packaging fixtures passed. Final controls and independent bars passed 97,497 source assertions across 438 configurations and 28,283 assertions in six native fixture cases. Those cases used inert provider/input fixtures: actual numeric-popup delivery, Rainmeter host unload/ancestor-exit integration and resource cost remain unverified. See [RAM live memory semantics](modules/RAM.md#live-memory-semantics-and-units) and [validation limits](modules/RAM.md#validation-results).

GPU's compact overview contains the queried card name, VRAM/type and base/boost clocks. A metadata query at load/refresh selects the discrete adapter through DXCore. When available, installed NVIDIA driver interfaces provide physical framebuffer capacity, memory type/maker and clocks after an exact adapter-identity match. Missing capabilities retain known values and explicit unknown fields; AMD/Intel vendor-specific providers are not implemented. No card specifications or screenshot values are stored as device profiles.

Below that overview, GPU memory reports physical framebuffer capacity minus currently available dedicated bytes as VRAM occupied/total, including driver-reserved memory, with a shared-thickness occupancy bar. A separate Shared RAM row shows DXCore shared-segment resident bytes, with committed bytes in its tooltip; these are independent accounting values and resident can exceed committed. Four NVIDIA activity readings report GPU, memory-controller, video and bus busy time over the preceding one-second interval. These independent percentages are never added or treated as memory-capacity buckets, Windows D3D utilization or byte bandwidth. The existing five process-engine rows remain at the bottom with their separate all-adapter scope. Each row also reports dedicated memory for its exact PID, LUID and physical adapter node through built-in UsageMonitor. Repeated engine rows can show the same allocation, and allocations shared between processes can be counted in more than one process, so these values are not additive and do not partition the total VRAM bar. Missing, unsupported or ambiguous zero observations display `VRAM: --`. The monitor enforces a 650 logical px minimum while preserving larger saved heights; its independent settings window remains 648 px, including separate Power and Clock source selectors.

Temperature, dedicated memory, shared memory, activity, board power and current graphics clock share the existing resident telemetry host and bounded temporary session, with independent feature statuses. Sampling stays at `Max(1000,MetricsInterval,SensorInterval)` milliseconds, capped at 30000. The exact 27-field `GPU_TEMP` v4 snapshot retains the preceding 25-field telemetry layout, including power status/milliwatts and clock status/graphics kHz, then appends a process-name request ID and bounded hex-encoded basename map. Identity, age and sequence checks remain. Invalid identity, stale samples or host failure clear the frame; individual unsupported features remain unavailable without suppressing supported readings. The host, lease and three bounded temp paths remain shared; this extension adds no per-sample process or dependency installation.

GPU's existing five process-engine rows can annotate their original raw PID/adapter/engine counters with executable basenames, omitting the final extension and falling back to PID. The graph requests at most five unique PIDs; sorted membership and a request generation travel in the existing five-second lease update. The same host uses limited process-query/synchronization access, retains at most five handles and releases dropped PIDs before opening replacements. Exited, denied or failed lookups remain unavailable while continuously requested, avoiding repeated reopening. Returned maps contain no executable paths; bounded UTF-8 names are checked against request generation, membership and session freshness before display. Names can lag by a lease interval plus a telemetry sample and do not establish counter freshness or the matching process lifetime. The counter identities, percentages, ranks and all-adapter scope remain unchanged. Backend source/ABI review, helper packaging and final default/narrow native checks passed, including actual resolved labels, advancing v4 snapshots and owned host/session cleanup. See [GPU process-name lookup](modules/GPU.md#process-name-lookup).

The five per-row memory lookups use exact raw counter identities and positive byte values only. UsageMonitor exposes neither a timestamp nor a way to distinguish a valid zero allocation from a missing named counter, so the display does not claim freshness or zero-byte precision. These measures add one in-process GPU Process Memory category worker at its independent one-second cadence. They add no helper process, driver, session file or protocol field. The 50 px row pitch places the memory label under each activity bar; Fit targets 650 px. See [per-application dedicated memory](modules/GPU.md#per-application-dedicated-memory).

GPU Settings remains 648 logical px, now with 61 meters: Columns uses integer 1–2 arrows and a typed center; Power, Clock and Hive wrap their choices; Fit remains an explicit command. Provider controls have no visibility-dependent details to collapse. Both final native endpoints passed 711 settings, 139 overview, 545 graph and 2,112 telemetry assertions and four real shared-helper Columns scenarios. The helper's unshown native controls exercised Enter/save/reload, Escape, invalid input and restoration, without claiming physical pointer delivery or visible-editor focus. Existing source semantics and explicit recovery remain; see [GPU final validation](modules/GPU.md#process-names-and-settings-steppers-validation).

The live graphics clock uses NVAPI's CURRENT selector and graphics domain, independently of refresh-only base/boost specifications. Direct board power uses an already installed 64-bit NVML library after complete inventories establish a unique PCI match to the selected card; its watts represent the driver-reported instantaneous or one-second-average consumption according to architecture. The 32-bit host leaves direct power unsupported while other supported features continue. `GPUPowerSource` and `GPUClockSource` default to literal `0` (Driver); literal `1` explicitly selects an HWiNFO override without automatic fallback. All nine Registry measures start disabled. Only a field whose source and `GPUEnableSensors` are both literal `1` enables its four-measure bank; shared enumeration enables when either bank is needed. Preserved mappings do not activate reads on their own. The separate UsageMonitor worker and selected HWiNFO exports retain their cadence and availability limits. See [GPU power and current clock](modules/GPU.md#direct-gpu-power-and-current-clock) and [memory/activity semantics](modules/GPU.md#gpu-memory-and-driver-activity) for validation and unverified hardware/performance cases. The corrected Lua RunCommand status/Stop path passed two isolated native checks: explicit Stop and an injected lease-write failure stopped the owned host within four seconds while the skin remained loaded, before the minimum 15-second lease expiry.

Visualizer retains one AudioLevel output parent and 24 measured bands at its existing 33/50/100 ms choices. Its current-output row places the user-selected 14 px Lucide Speaker vector in `MediaColor` before the clipped device name, with a 4 px gap. The combined transparent row target sends Win7Audio `ToggleNext` on click or wheel down and `TogglePrevious` on wheel up, clears a custom `VisualizerDeviceID`, then refreshes so the single AudioLevel parent follows the new Windows default output. This can redirect system audio; it sends no volume or mute command. The row owns the existing consolidated tooltip, so the icon adds no separate target. The decoded SVG, provenance and full ISC notice ship under `@Resources/Modules/Visualizer/Icons/Lucide/`; Rainmeter draws the adapted native Shape and does not load the SVG. A separate native Shape mask builds 24 measured top-rounded paths over one fixed solid or LinearGradient fill. Media, Accent 1 and Accent 2 remain solid modes; `VisualizerColorMode=3` is the horizontal Accent 1-left to Accent 2-right gradient, and mode 4 adds a vertical Accent 1-bottom to Accent 2-top gradient fixed to the usable plot. Bundled `Color.lua` applies color choices only on refresh, with no Update callback or file access. The icon, actions and renderer add no provider or process, though native path parsing and compositing follow spectrum updates and remain unbenchmarked.

Audio Settings is 400 logical px with eleven arrow/value controls: seven numeric and four categorical. Width, palette, quality and interval wrap their categories; numeric arrows select neighboring presets without wrapping. Typed integers accept height only at 126/146/186, bar gap 0–4, top radius 0–12, axis gap 0–24, sensitivity 10–80 dB, attack 0–2000 ms and decay 0–5000 ms. The axis displays 0, minus half sensitivity and minus sensitivity as a relative AudioLevel scale, not calibrated dBFS, SPL or Windows volume. Derived main height grows only when a short saved height plus axis gap would leave less than 54 logical px of usable bars; the saved PanelHeight remains unchanged. Shared DataBarThickness does not apply to FFT geometry. The shared input helper runs only for explicit numeric entry. See [Visualizer controls and validation](modules/Visualizer.md) for source, native rendering, helper evidence and performance limits.

## Files and settings

Every config joins `[Rainmeter] Group=Parallax` and supplies a context action opening `Parallax\Settings`, `Settings.ini`. Include these files in this order from a `[Variables]` section using numbered `@Include` keys:

1. `#@#Defaults.inc` (shared defaults).
2. `#@#User\Settings.inc` (persisted global choices).
3. Optional `#@#User\<Module>.inc` (module-owned overrides/defaults).
4. `#@#Geometry.inc` (derived dimensions).
5. `#@#Styles.inc` (shared meter styles).

Set `Columns=1` or `Columns=2` and `PanelHeight=<logical pixels>` BEFORE Geometry.inc, either in module User variables or the entrypoint. Do not override a user's module choices later in the entrypoint. Includes contain their own section headers. Rainmeter merges layered `[Variables]` sections by key. Keep include identifiers unique in an entrypoint. Use `@Resources/Modules/<Module>/` for module scripts and includes.

Global changes persist through `!WriteKeyValue` into `@Resources/User/Settings.inc`, then refresh only `Parallax` configs using `!RefreshGroup`. Module settings may override global colors, fonts and `Scale` locally in their own User file. All persistable settings live in `[Variables]` for Skin Packager variable preservation. Ship every supported user key with a default. New keys must retain useful defaults during upgrades. Runtime data (timer state, queue caches, OAuth tokens) is separate from distributable configuration, with its persistence and migration documented by the module owner. No OAuth material in skins.

Global Settings groups appearance controls under Appearance and refresh presets/cadence under Performance. The Refresh field now wraps Balanced/Economy through previous/next arrows and a forward-cycling center. Its current label is inferred from all four saved intervals; unmatched values show Custom and remain untouched until an explicit choice. From Custom, previous selects Economy and next/center selects Balanced. No profile key, helper, collection process or layout-height change is added. Utility panels follow the [shared settings layout](UTILITY-SETTINGS.md), using named sections appropriate to their features and preserving saved monitor geometry. Settings section headings use Accent 1, while their values use body text. When a saved visibility choice hides a utility feature, collapse its dependent settings details and reclaim their space, keeping a heading and accessible visibility control. Restore existing values when shown again. Provider failures and unloaded skins do not define this visibility state; setup and recovery remain accessible.

The global Theme dropdown currently contains only Default. Its 37-key bundle matches shipped defaults and includes the theme identifier, font family and all three text sizes/colors, both accents, panel/background/divider/table-header styling, data bar thickness, padding/corners, graph styling, status colors and utility metric colors. It refreshes Parallax once. The theme leaves suite scale, column width, snap gap, cadence and module-owned includes intact; module appearance overrides retain precedence. `Theme=default` records the last applied base theme and is preserved with other global variables. [SETTINGS.md](SETTINGS.md) lists the exact bundle and controls.

Numeric fields accept scale 75–200%, column width 180–320 px, gap 0–16 px, rounding 0–24 px, title size 6–12 pt, header/body size 6–10 pt, background transparency 0–100%, border/divider/table-header thickness 0–4 px, and data bar thickness 1–12 px. Width/gap/rounding require whole pixels; other fields allow up to two decimals and optional appropriate unit suffixes. The helper receives only known keys and canonical launch values. RunCommand returns a fixed ASCII numeric/cancel protocol, and a fixed FinishAction calls Lua to read the result as data and validate it again. The helper writes no files; accepted changes use the shared persistence path above.

`BackgroundTransparency` is an input operation, not a persisted variable. It preserves background RGB and writes the nearest 8-bit alpha into `BackgroundColor`; zero percent is opaque and 100% fully transparent. The displayed value is derived from the saved alpha and rounded to two decimals: 37.5% becomes alpha 159, then displays 37.65%. The Default theme restores `BackgroundColor=15,15,15,255`.

## Geometry

`ColumnWidth=220` is the painted width of one panel at scale 1. This adopts the CPU meter's former 200+20 px width as the shared standard; CPU now uses the same geometry without an additive width override. `Gutter=8` is the visible gap between edge-snapped panels, included as equal transparent outer margins. `Scale=1` initially. These are editable choices, not a Rainmeter hardcoded snap distance.

- `UnitWidth = Round(ColumnWidth * Scale)`.
- `Gap = 2 * Round(Gutter * Scale / 2)` (even physical pixels).
- `Inset = Gap / 2`.
- `Pitch = UnitWidth + Gap`.
- `WindowWidth = Columns * Pitch`.
- `PanelWidth = WindowWidth - Gap`.
- `WindowHeight = Round(PanelHeight * Scale) + Gap`.

A single panel paints 220 px inside a 228 px window. A double panel paints 448 px inside a 456 px window: `2 * 220 + 8 = 448`. Two adjacent single windows occupy precisely the same width as one double window. Draw the panel at `(Inset, Inset)` and size it `(PanelWidth, PanelHeightPx)`. Use an invisible bounds meter (StyleBounds) so transparent margins remain part of the skin window. Do not add meters outside the declared window bounds. `StylePanel` draws the surface. Content starts at `ContentX/ContentY`; usable width is `ContentWidth`.

Use logical vertical positions multiplied by `#Scale#`, measured relative to `#Inset#`. Text should clip/truncate long content without growing the window. Use fixed declared widths; do not allow cover art or labels to change snap geometry. Width variants may share an entrypoint with a persistent Columns setting.

Title/icon rows share a vertical center: `TitleRowCenterY=Inset+15*Scale` and `TitleRowHeight=22*Scale`. Layer `StyleTitleRow` after `StyleTitle` for `LeftCenter` anchoring; intentionally centered titles use `CenterCenter`. A title-only settings panel uses the same center without adding an icon. Existing title-row identity icons use `TitleIconScale=Scale*TitleFontSize/10`, a nominal `TitleIconSize=14*TitleIconScale`, and `TitleIconGap=4*Scale`. Scale actual paths, shapes and strokes, then center their canvas on the title row. Title X and available width must reserve the resulting icon size, gap and any right-side control/readout. Media's Monitor Play title icon follows this contract. Its Audio Lines player icon stays 14 logical px and scales only with the suite; it shares the title center in double-width mode and the stacked player-row center in compact mode. Offset surfaces translate the shared center by their surface offset. Fixed-size gear/close controls keep their click targets and align to the row center. Keep following content clear as title sizes change; do not rely on enlarging a Shape meter's W/H to scale its artwork.

Rainmeter `SnapEdges` is a per-skin setting in Rainmeter.ini, not an arbitrary spacing option. Leave normal Rainmeter snapping enabled; a user can disable it or hold Ctrl while dragging. Validate single/double alignment at 100%, 125%, 150% and 200% suite scale and mixed Windows DPI before release. See [official skin settings](https://github.com/rainmeter/rainmeter-docs/blob/master/source/manual/settings/skin-sections.html).

## Appearance

Shared typography uses `FontFace`, `TitleFontSize`/`TitleTextColor`, `HeaderFontSize`/`HeaderTextColor`, and `FontSize`/`TextColor` for body text. Defaults are IBM Plex Sans, 10/8/9 pt, and RGB 220/220/220 for titles/body versus 175/175/175 for headers. `MutedColor` remains available for secondary information. `AccentColor` defaults to 137/190/250; `AccentColor2` defaults to 181/161/226 for secondary actions. Utility metric colors and `GoodColor`/`WarningColor`/`DangerColor` preserve their semantic roles rather than following general text/accent changes.

Surface variables are `BackgroundColor`, `BorderColor`, `BorderThickness`, `DividerColor`, `DividerThickness`, `TableHeaderBorderColor`, `TableHeaderBorderThickness`, `DataBarThickness`, `TrackColor`, `PanelPadding` and `CornerRadius`; graph background/grid/height retain their separate variables. `StylePanel` draws its border inside the panel using the configured thickness times Scale. `StyleRule` uses divider color and thickness times Scale. `StyleTableHeaderRule` uses the independent table-header border color and thickness, defaulting to `50,50,50,255` and 1 logical px. Thickness zero hides the corresponding stroke. CPU thread/process header underlines use these table-header keys with local geometry; their footer lines remain section dividers. Global Settings uses the shared header style beneath its Panel style and Text settings column headings. Scalar utilization, capacity and progress bars use `DataBarThicknessPx=(Max(1,Round(#DataBarThickness#*#Scale#)))` from shared geometry; the default is 6 logical px, matching CPU bars. Modules reserve clearance through the supported 1–12 px range without changing collection cadence. Graph frames, grids, telemetry traces and equalizer band geometry keep their separate styling.

Shared styles are `StyleBounds` (Image), `StylePanel` (Shape), `StyleTitle`, `StyleHeader`, `StyleLabel`, `StyleValue`, `StyleButton` (String), `StyleRule` and `StyleGraph` (Shape). Use `StyleTitle` for panel titles, `StyleHeader` for section/table headings, and body styles for labels and live values. `StyleSecondaryButton` is a color-only override layered after `StyleButton` for Accent 2. Every actual meter must declare `Meter=<type>` as well as `MeterStyle=<style>`. Styles contain no Meter key so they do not draw independently. Set meter-specific X/Y/W/H after applying the style when appropriate, and remove local font overrides that would bypass the intended typography role. Buttons require descriptive ToolTipText; statuses must use text as well as color.

Value text in independent utility settings panels uses the body `TextColor`, including clickable values, toggles, numeric values and available choices. Being clickable does not make a setting value an accent-colored action. Command/navigation buttons retain their action colors; swatches and semantic diagnostic/disabled states keep their own roles. This color mapping does not change a panel's existing font sizes, geometry or user preferences.

Independent utility settings panels include `@Resources/UtilitySettingsNote.inc` after their background panel meters. Set the local `SettingsUtilityName` before geometry. Its two compact lines identify the utility and link to `Parallax\Settings`, `Settings.ini`, without saving settings or refreshing utilities. The note occupies logical Y=32–68 relative to Inset; reserve this area below the title and keep settings controls below it. Text uses body size minus 1 pt, body color for context and Accent 1 for the navigation link. It adds no measures or polling.

Global Settings and ColorPicker deliberately retain neutral control text while global text colors are edited. Global Settings form labels/values and table column labels use fixed 9/8 pt editor sizes scaled with the suite; its section names use Accent 1 and its main title still inherits title typography. Independent utility section names also use Accent 1 with HeaderFontSize. ColorPicker keeps neutral text colors and an opaque neutral surface/border, while its fonts still inherit suite typography. These editor exceptions do not replace module appearance overrides. Module layouts must honor the supported global size ranges without silently capping them locally.

## Data and update policy

Default `MetricsInterval=1000` ms, `SensorInterval=2000` ms, `CapacityInterval=30000` ms. These are targets for native measures whose cadence can be controlled. Use UpdateDivider for slow data; static metadata updates on refresh. UsageMonitor has independent 1 Hz workers per category; slowing skin updates does not reduce those workers. Optional categories/skins must be unloaded to remove that cost.

Keep visualizer in a separate config, default `VisualizerInterval=50` ms (20 FPS), optional 33/100 ms. It captures mixed output-device audio, not solely Spotify. Never run all skins at visualizer frequency. Low power settings do not claim to throttle third-party provider polling. No timer writes every second; save state at user actions/checkpoints as needed.

No provider means unavailable, not measured zero. Distinguish disconnected, stale, unsupported and valid data when the provider exposes enough evidence. Sample timestamps/labels where available; do not invent freshness detection a provider cannot support. Document unavoidable ambiguities.

## Acceptance

Each module delivers usable baseline controls/data within its capability, configuration instructions, source attribution, meaningful checks, and known limitations. Record prototype versus verified live behavior. Test reload persistence, missing providers, long labels, dimensions at multiple scales and CPU cost with helpers included before release. The shared settings skin can close independently without stopping other modules.
