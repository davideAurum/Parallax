# Native Parallax screenshots

`Preview-Parallax.ps1` creates actual Rainmeter window captures from an existing Rainmeter installation. It copies the whole suite into a unique build directory, then loads Settings, Chronometer, CPU, RAM, GPU, IO, Network, Media Setup, and Visualizer in a separate instance using its own absolute INI and existing Skins directory.

```powershell
.\tools\Preview-Parallax.ps1
.\tools\Preview-Parallax.ps1 -Modules Settings,CPU,RAM -Scale 0.75 -ColumnWidth 180
.\tools\Preview-Parallax.ps1 -Modules Settings,CPU,RAM -ColumnWidth 220 -Columns 2
.\tools\Preview-Parallax.ps1 -Modules Settings,ColorPicker
.\tools\Preview-Parallax.ps1 -Modules Settings,ColorPicker -TitleFontSize 12
.\tools\Preview-Parallax.ps1 -Modules IO
.\tools\Preview-Parallax.ps1 -Modules Chronometer,RAM -UtilitySettings -Scale 1 -ColumnWidth 220 -TitleFontSize 12
```

Default settings come from the staged source, currently a 220 logical-pixel column and IBM Plex Sans. `-Scale` accepts 0.75, 1, 1.25, 1.5, or 2; `-ColumnWidth` accepts 180, 200, 220, 240, 280, or 320. Both change only staged global settings, and intentional module overrides retain their normal precedence. `-Columns 1` or `-Columns 2` explicitly changes each selected utility's staged User include. Settings always remains double width; the runner verifies the effective native Columns value. Omitting `-Columns` preserves source utility choices.

`-UtilitySettings` selects each utility's independent `<Module>\Settings\Settings.ini` panel instead of its monitor. Global Settings and ColorPicker retain their usual entrypoints. The runner verifies both the active config and file, recognizes module-specific settings bounds meters, and records the actual captured window size. A utility settings window remains double width independently of the monitor's saved geometry. When combining this switch with `-Columns`, that option changes only the staged monitor preference; its value is not asserted as the settings window's effective Columns value.

`-TitleFontSize` accepts 6–12 pt and changes only the staged global title size, for checking title/icon scaling and vertical alignment. Omitting it preserves the source preference. Bundled TTFs and their license files are copied by staging with the rest of the suite. They are loaded by Rainmeter from the isolated skin resources; the runner performs no system font installation. Native reports record the configured FontFace; visual inspection is still needed to verify typography.

Media's title uses Monitor Play with the shared title-size/center contract. Its player identity uses body font size and a fixed 14 logical px Audio Lines icon, scaled with the suite rather than title size. The current double-width layout places that identity beside the title; compact mode retains the stacked row. At width 220 and scale 1 the collapsed Player windows remain 456×222 double and 228×210 compact. The earlier icon correction passed 80 source layouts and 1,845 native assertions at two maximum-font endpoints. Root inspected both `synthetic-IdentityPlayerMax-max-w180-s0.75-c1.png` and `synthetic-IdentityPlayerMax-max-w220-s2-c2.png` under `%TEMP%\Parallax-MediaSettings-test-b0501cfc76d447b983aa6e011f9aa3fa` and matched Common/Header/new SVG hashes. This is synthetic visual evidence; the integration preview's ordinary Media selection still loads Setup without connecting a provider.

Media's subsequent anchored-artwork fixture is `%TEMP%\Parallax-MediaSettings-test-b42017494bd24b96971def88a42f8bac`. Root inspected `synthetic-ArtworkPlayer-max-w220-s1-c2.png`: the 456×222 double-width window contains 169×169 artwork at the original top-left, wide metadata/progress/buttons raised 20 px, current-player identity beside the title and the queue divider at its prior y189 anchor. The focused run passed 39 layouts and nine captures over widths 180/220/320, scales 0.75/1/2, compact/double, maximum fonts, queue states and artwork availability with zero Rainmeter errors. A targeted live refresh confirmed the same geometry without issuing playback commands.

Selecting IO loads Disk Meter through `IO-Disk.ini`, showing the saved drive selection (C: by default), per-drive decimal capacity/read/write speeds and one scrolling history graph in the saved C:/Combined/Overlay mode. The display rename leaves module paths and runner arguments unchanged. The retained `-IOVariant` parameter accepts only `IO-Disk.ini` and defaults to it. The runner verifies the selected active file. Native bounds reporting recognizes `MeterBounds` and IO's `MeterIOBounds`, records the declared geometry variables, and captures the actual Windows window dimensions independently. The graph fills only with observations collected after load; a short preview does not show a full history.

`-SettleSeconds` controls the brief initialization delay (4–30 seconds, default 8). History graphs initially have only that many samples. Capture while source edits are paused to obtain a coherent review snapshot.

Selecting RAM loads Memory Meter through the unchanged RAM config path. Its overview wraps to the native text height and moves the RAM/PAGE pairs, process table and bottom graph. The inventory query needs normal Windows CIM access; an inherited restricted execution context can correctly produce Unavailable even when the normal Windows query succeeds. The 2026-09-12 normal-access sample at width 220, scale 1, title 12 and one column rendered real inventory with a 228×327 window, zero Rainmeter errors and no observed clipping. Evidence is `build/Parallax-visual-preview-20260912T235105404Z-cfa20304/captures/RAM.png`. This is one inspected layout, not a provider accuracy or performance benchmark.

The preceding independent Chronometer and RAM settings were captured with `-UtilitySettings` at width 220, scale 1 and title 12. Their then-current windows measured 456×788 and 456×320 respectively, with no Rainmeter errors or warnings. Both captures were inspected for title/close clearance, section rules, label/value alignment and clipping. Evidence is `build/Parallax-visual-preview-20260913T005255691Z-e62a9656/captures/Chronometer.png` and `captures/RAM.png` in the same run. Later controls and RAM's independent PAGE bar supersede these settings dimensions; the captures remain historical evidence.

A later Chronometer capture verifies its conditional layout and Accent 1 headings with the saved Timers-hidden choice: `build/Parallax-visual-preview-20260913T011816053Z-0b76b89f/captures/Chronometer.png`. At width 220, scale 1 and title 12, the window is 456×544 rather than the full form's 456×788. Root inspected the retained Timers heading/show control, compact footer, other sections and title/close without clipping. No Rainmeter errors were recorded. This capture covers the Timers-hidden mask; the module's native tests cover other hide/show combinations and the all-hidden initial layout.

The runner starts its instance hidden, places skins offscreen with normal alpha, and enumerates windows belonging only to the process it launched. It calls Windows `PrintWindow` on those Rainmeter window handles. It never captures the desktop, other processes, or the user's existing Rainmeter instance; it never sends command-line bangs. It terminates only its own process in `finally`, leaving files for review.

The output contains individual PNG files under `captures`, native per-config reports, `capture-report.json`, `preview-report.json`, and `Rainmeter.log`. Inspect the images directly. A color-count heuristic detects likely blank captures but cannot establish visual quality; rendering may vary across Windows/Rainmeter versions. The actual data and honest missing-provider states remain visible. Media defaults to the optional-provider setup variant and no provider is installed or connected. The spectrum may legitimately be quiet. ColorPicker is an optional preview selection, always double width like Settings; `-Columns` affects neither settings utility.

**Do not distribute a preview directory:** it contains test instrumentation, and its original stage manifest predates those additions and preview-only settings. Run the regular staging command again when preparing a release.

Media's load-time recovery is restricted to the normal `%APPDATA%\Rainmeter` settings path. This runner's isolated INI profile therefore reads no saved provider intent and launches no Source/Queue recovery helper, even if the user enabled them in the normal profile. Media Settings has no lifecycle include. Passive readers may still show existing data as described by the module; a preview is not a provider restart test. The lifecycle's 12 native scenarios separately verify this profile guard without executing provider commands.

This is visual smoke evidence, not a performance benchmark, accuracy test, long history, mixed-DPI test, or proof of optional-provider support. The capture uses [Microsoft's PrintWindow API](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-printwindow); layered-window support must be confirmed from actual resulting pixels on the current host.


The shared numeric/cycling-control revision was captured from a Global-only source fixture, avoiding the utilities being edited concurrently. `build/global-stepper-preview-source-20260913T021656609Z-2f1d9a94` contains only Global Settings, shared includes/scripts/fonts and copied preview tooling. Its default capture is `build/Parallax-visual-preview-20260913T021659029Z-506f81ba/captures/Settings.png`; its narrow capture is `build/Parallax-visual-preview-20260913T021707626Z-0ae9c007/captures/Settings.png`. Both were inspected: all twelve arrow/value/frame groups fit at 456×766 and 282×575 respectively, section headings use Accent 1, and Default retains its dropdown with disabled previous/next controls. Both native logs have zero warnings/errors and the two owned processes exited. This fixture is test evidence only, not a suite release stage.

Network now loads one canonical monitor with connection/adapter details above traffic and histories. Its effective height is `Max(Round(PanelHeight*Scale),Round(464*Scale)+DataBarThicknessPx)`, giving 228×478 at the default single width or 456×478 double. The old Connection config is a no-measure compatibility redirect and is not a preview target. Its completed numeric/cycling settings retain a 598 logical px baseline. Inspected default/narrow captures are in `@Resources/Modules/Network/QA/settings-run-54b5c5607233468fb1f1bc5567ebabab`, measuring 456×606 and 282×455. The Network module report records combined-monitor, controller and typed-input evidence.

RAM's final six-control settings were separately captured from the settings-only fixture `build/ram-stepper-preview-source-e1018c36e5544467ae90f6690c77ade9`. The nested `build/Parallax-visual-preview-20260913T023712680Z-4391f6a7/captures/RAM.png` shows all controls at width 180/scale 0.75 in a 282×261 window. Root inspected the aligned arrows, numeric values, visibility fields and footer with zero Rainmeter errors. This fixture starts no RAM/PAGE provider. The final module fixture runs cover six main/settings layouts and independent bar behavior; see the RAM report for the separate resident-provider checks and limits.

Disk Meter's final `build/Parallax-io-settings-smoke-20260913T022921714Z-8a5d75de` captures show the main at 228×315 and independent settings at 456×586. Root inspected the one-line read/write/capacity cells beneath the bars, Split graph and settings controls. The subsequent narrow None/Both/Split run is `build/Parallax-io-settings-smoke-20260913T023306190Z-3119e5e7`, with 141×124 main and 282×419 settings (440 px after restoring All). Captures reflect the fixture's state at capture time; `IO-history.png` in the first run follows a names-mode action to Letters. Native logs have zero errors. The IO module report records action, geometry and provider limits.

The final GPU default/narrow runs are `build/gpu-native-smoke-20260913T023329718Z-9f7a505b` and `build/gpu-native-smoke-20260913T024135525Z-43695f1f`. Independent review inspected settings at 456×656 and 282×492, plus the settled narrow monitor at 141×426. Source hashes match the tested copies, four real typed scenarios pass per run, and both logs have zero errors and one harmless already-active warning. Displayed process names remain optional delayed annotations with PID fallbacks. See the GPU report for full native counts and lifecycle limits.

The subsequent per-application VRAM captures are in `build/gpu-native-smoke-20260914T005529029Z-da2d4e8e` and `build/gpu-native-smoke-20260914T005702060Z-7715b368`. Root inspected the settled default 456×658 and narrow 141×494 monitors plus their 456×658 and 282×492 settings windows. Dedicated-memory text clears each shared-thickness activity bar, all five 50 px rows remain contained, and Fit uses the 650 px logical minimum. Both logs have zero errors and one harmless already-active warning. These captures show exact live counter bindings and positive byte values on the tested computer; they do not establish additive totals, zero-byte precision, freshness, other hardware or sustained overhead.

Media and Visualizer also completed independent review of their settings captures and source agreement. Media retains 498/554 logical px conditional settings. Visualizer's current 400 px form has eleven arrow/value groups; `build/Parallax-visualizer-gradient-stepper-eae226812941443dbe64b5899689d5e0/captures/Visualizer.png` is the inspected 456×408 default form. Root also inspected the real 456×154 vertical spectrum at `build/Parallax-visualizer-gradient-stepper-c1bfb7cbb7394ba99394283ab67d7a9d/captures/Visualizer.png`. Synthetic graphics under `build/visualizer-shape-fixtures-e86b39f44b644597800cc1b74b07378c` cover the 141×118 narrow minimum-height/max-gap case, gradient directions, top rounding, relative dB ticks and clear/hide/restore transitions. Current logs are clean. Module reports preserve exact evidence paths and do not claim physical mouse delivery, calibrated audio, mixed-DPI verification or renderer performance.

Visualizer's later Speaker-icon captures are `build/Parallax-visualizer-gradient-stepper-19cf1c41a28b4318a697e18d63d3de4a/captures/Visualizer.png` at 456×154 and `build/Parallax-visualizer-gradient-stepper-fea526be40694c1c99ca181c9ba64a5d/captures/Visualizer.png` at 141×118. Root inspected both. The 14 px Media-color vector, 4 px gap and clipped device name retain the output row's right edge and both panel heights. Logs contain zero warnings/errors; the source manifest records the exact supplied SVG and native Shape bytes.

The output-switch revision's fresh render is `build/Parallax-visualizer-gradient-stepper-a11c65d8f0ff468da207ec767607adde/captures/Visualizer.png` at 456×154. Root inspected it and matched the final View, Audio and validator hashes; the log is clean and ColorSuite passes 64 cases. Static tests verify the exact next/previous commands, preference clearing, refresh and absence of volume/mute actions. The preview did not execute the switch because that would change the user's live Windows default output.

The final Global Settings Refresh profile row was captured from `build/global-profile-preview-source-9b84568ee2204704ae1fc5aa322b1793`. Nested runs `build/Parallax-visual-preview-20260913T031024352Z-8b25eb46` and `build/Parallax-visual-preview-20260913T031031626Z-02bf8964` contain inspected default/narrow `captures/Settings.png` files at 456×766 and 282×575. Balanced is centered between its arrows and all existing fields retain clearance. Both logs have zero Rainmeter errors, source/fixture hashes match, and the owned processes exited. The focused fixture intentionally omits other modules, producing expected missing-module static warnings during fixture staging; the final whole-suite static check has zero errors or warnings.

Chronometer's final nine-group settings received an independent integration pixel review from `build/chronometer-settings-review-source-2c5a9d487c1f47bc8d43e972ffcc9b59`. All-visible default/narrow captures at maximum 12/10/10 pt fonts and four-pixel borders/dividers measured 456×760 and 282×570. Nested default/narrow runs are `build/Parallax-visual-preview-20260913T031027903Z-6c688dee` and `build/Parallax-visual-preview-20260913T031027389Z-6687d444`; each contains `captures/Chronometer.png`. The fixture's `capture-review.json` records accepted pixels, matching production/fixture/stage hashes, zero Rainmeter errors and the two owned process IDs, which both exited. These settings-only captures launch no main timer, provider, event editor or numeric popup. Collapse/actions and the headless typed path retain the separate owner evidence.

CPU's final settings-only captures are recorded in [its module report](../docs/modules/CPU.md#settings-structure-collapse-and-stepper-validation-2026-09-12). Expanded/collapsed windows measured 456×1008/456×896 at default, 282×756/282×672 at narrow and 912×2016/912×1792 at 200%. The owner inspected all endpoints, and root additionally inspected the final expanded 200% image at `@Resources/Modules/CPU/tests/.runtime/cpu-settings-50d13a9a5f9f469d9c020aded238c3b7/expanded.png`. Fields, five Accent 1 headings and setup instructions fit. These fixture captures launch neither the main CPU nor its discovery/input providers.
